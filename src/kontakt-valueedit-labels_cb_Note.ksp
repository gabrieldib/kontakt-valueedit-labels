on note
    ignore_event(EVENT_ID)
    set_event_par(EVENT_ID, EVENT_PAR_0, 1)
    inc(note_held_count)

    USE_CODE_IF(__USE_ARP)
        import "ARP/__ARP_05_note_cb.ksp"
    END_USE_CODE
    
end on