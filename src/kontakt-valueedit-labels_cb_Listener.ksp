on listener
    // if (NI_SIGNAL_TYPE = NI_SIGNAL_TIMER_BEAT)
    //     message("juicy code for listener in BEAT mode")
    // end if    
    // if (NI_SIGNAL_TYPE = NI_SIGNAL_TIMER_MS)
    //     message("juicy code for listener in MS mode")
    // end if
    USE_CODE_IF(__USE_ARP)
        import "ARP/__ARP_07_listener_cb.ksp"
    END_USE_CODE
end on
